{
  "name": "skua-cli",
  "version": "0.2.2",
  "description": "A text processing language in the order of awk",
  "main": "./bin/skua.sh",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matt Neary",
  "dependencies": {
    "ramda": "0.17.1",
    "rx": "2.5.3",
    "rx-node": "1.0.1"
  },
  "scripts": {
    "prepublish": "make"
  },
  "bin": {
    "skua": "./bin/skua.sh"
  }
}
